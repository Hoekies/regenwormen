services:
  - type: web
    name: regenvormen-server
    runtime: node
    buildCommand: npm install && npm run build --workspace=packages/shared && npm run build --workspace=server
    startCommand: node server/dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
    plan: free
