services:
  - type: web
    name: regenvormen-server
    runtime: node
    buildCommand: npm install
    startCommand: npm start --workspace=server
    envVars:
      - key: NODE_ENV
        value: production
    plan: free
